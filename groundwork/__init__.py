"""groundwork — standalone recon → map → plan pipeline for any repo or local codebase.

Lifted from Pre Atlas's audit tooling (build_system_index.py + _build_map.py) and
de-hardcoded so it runs against any repo, with no Claude Code harness required.
"""

__version__ = "0.1.0"
